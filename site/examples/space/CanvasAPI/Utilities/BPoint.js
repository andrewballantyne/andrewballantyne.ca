/**
 * A simple object wrapper for a single X and Y coordinate.
 *
 * @param x {Number} The X coordinate
 * @param y {Number} The Y coordinate
 */
function BPoint ( x, y ) {
	this.x = x;
	this.y = y;
}
